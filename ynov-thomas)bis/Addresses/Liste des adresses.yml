info:
  name: Liste des adresses
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/api/addresses
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
