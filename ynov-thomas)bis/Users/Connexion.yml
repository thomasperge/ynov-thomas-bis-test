info:
  name: Connexion
  type: http
  seq: 2

http:
  method: POST
  url: http:/localhost:3000/api/users/tokens
  body:
    type: json
    data: |-
      {
        "email": "test@test.com",
        "password": "supersecret"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
