{
  "name": "ynov-thomas-bis-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "npm run dev --prefix server",
    "test": "npm test --prefix server",
    "test:all": "npm test --prefix server",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
