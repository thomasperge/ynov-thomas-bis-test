info:
  name: Create User
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/users
  body:
    type: json
    data: |-
      {
        "email": "thomaskauffmant74@gmail.com",
        "password": "toto"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
