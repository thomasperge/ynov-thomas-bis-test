info:
  name: Tokens
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/users/tokens
  body:
    type: json
    data: |-
      {
        "email": "thomaskauffmant74@gmail.com",
        "password": "toto"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
